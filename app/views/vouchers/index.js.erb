$('#modal').html('<%= escape_javascript(render "vouchers/index",
                                        :course => @course,
                                        :vouchers => @vouchers) %>');
$('#modal').modal();
<%= render 'shared/modal_flash' %>
window.initAjaxCSRF('<%= form_authenticity_token %>');