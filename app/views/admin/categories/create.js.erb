<% if @success %>
  $(window).hashchange();
<% else %>
  $('#modal').html('<%= escape_javascript(render "admin/categories/new",
                          :category => @category) %>');
  <%= render 'shared/modal_flash' %>
<% end %>
window.initAjaxCSRF('<%= form_authenticity_token %>');