// Go to the first page
location.hash = '<%= raw hash_path(course_announcements_path(@course, :type => params[:type],
                                   :subtype => params[:subtype], :touch => Time.now.to_i)) %>';

window.initAjaxCSRF('<%= form_authenticity_token %>');