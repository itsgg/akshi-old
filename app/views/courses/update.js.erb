<% if @success %>
  $(window).hashchange();
<% else %>
  $('#course_<%= @course.id %>').html('<%= escape_javascript(
                          render "courses/edit", :course => @course) %>');
  <%= render 'shared/page_flash' %>
<% end %>
window.initAjaxCSRF('<%= form_authenticity_token %>');