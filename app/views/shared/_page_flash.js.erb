$('#flash .flash').remove();
$('#flash').html('<%= escape_javascript(
                      render :partial => "shared/flash_messages",
                             :locals => {:flash => flash}) %>');
$('#flash .flash').slideDown();
$('#flash .flash').delay(3000).slideUp();
