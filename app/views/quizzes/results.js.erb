$('#content').html('<%= escape_javascript(
                        render "quizzes/results", :quiz => @quiz,
                               :course => @course, :quizzes => @quizzes,
                               :scores => @scores) %>');
<%= render 'shared/page_flash' %>
window.initAjaxCSRF('<%= form_authenticity_token %>');